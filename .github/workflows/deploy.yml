name: Deploy Portfolio to GitHub Pages

on:
  push:
    branches: [master]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "24"
          cache: "npm"

      - name: Install dependencies
        run: npm ci

      - name: Remove server route handlers (incompatible with static export)
        run: rm -rf app/api

      - name: Build (static export)
        run: npm run build
        env:
          # AI / admin API is hosted separately on Vercel, not on GitHub Pages.
          # Replace with your actual Vercel deployment URL.
          NEXT_PUBLIC_API_BASE: "https://irmhanif-github-io.vercel.app"

      - name: Add .nojekyll (prevent GitHub Pages from ignoring _next folder)
        run: touch out/.nojekyll

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: out/

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
